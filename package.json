{
  "name": "research-notes-manager",
  "version": "1.0.0",
  "description": "Full-stack Research Notes Manager",
  "scripts": {
    "build": "cd server && npm install && cd ../client && npm install && npm run build && cd .. && mkdir -p server/public && cp -r client/dist/* server/public/",
    "start": "cd server && npm start",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
